# Mythline v2 — Root environment variables
# Copy to .env and fill in values.
# This file aggregates all variables across all services.
# Each service also has its own .env.example with only what it needs.

# --- LLM Provider ---
# API key for your LLM provider (e.g., OpenRouter)
# Agents read this via pydantic-ai's provider integration
OPENROUTER_API_KEY=<your_openrouter_api_key>

# Full pydantic-ai model identifier (provider:model)
LLM_MODEL=openrouter:openai/gpt-4o-mini

# --- Embedding ---
# Provider: openai_compatible | ollama | sentence_transformers
EMBEDDING_PROVIDER=openai_compatible
EMBEDDING_MODEL=openai/text-embedding-3-small
EMBEDDING_API_URL=https://openrouter.ai/api/v1/embeddings
EMBEDDING_API_KEY=<your_embedding_api_key>

# --- SurrealDB ---
SURREALDB_PORT=8010
SURREALDB_USER=root
SURREALDB_PASS=root
SURREALDB_NAMESPACE=mythline
SURREALDB_DATABASE=world_lore

# --- RabbitMQ ---
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672
RABBITMQ_USER=mythline
RABBITMQ_PASS=mythline

# --- MCP Service Ports ---
MCP_STORAGE_PORT=8005
MCP_WEB_SEARCH_PORT=8006
MCP_WEB_CRAWLER_PORT=8007

# --- Search ---
SEARCH_MAX_RESULTS=10
